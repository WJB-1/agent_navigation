### **🏁 Phase 2 核心验收标准清单**

#### **1\. 地图底座无错渲染 (对应模块 2.2)**

* \[ \] **Bug 修复确认**：按 F12 打开浏览器控制台，确认之前截图中出现的 AMap.Scale is not a constructor 报错已经完全消失。  
  * *⚠️ 警报提示：* 我注意到你上传的 README.md 底部依然写着 \<script src="...\&plugin=AMap.Walking"\>\</script\>。请务必检查 frontend/index.html 的实际代码中是否已经补全了 AMap.Scale,AMap.ToolBar 等插件。  
* \[ \] **地图呈现**：地图能够铺满指定的容器区域，支持鼠标拖拽平移和滚轮缩放。

#### **2\. LLM 配置链路闭环 (对应模块 2.1)**

* \[ \] **状态回显**：刷新页面时，配置面板能自动请求 GET /api/config/llm/active，并正确显示后端当前保存的激活模型（如：当前已激活：Gemini）。  
* \[ \] **保存联调**：在面板中修改 API Key 或模型并点击“保存配置” 后，网络面板 (Network) 显示请求后端 POST /api/config/llm 成功，且页面状态立即更新。

#### **3\. 视障语义 POI 与街景交互 (对应模块 2.3)**

* \[ \] **动态加载**：当你在地图上拖拽平移结束后 (moveend 事件)，网络面板应该能看到发起向后端 3002 端口的 GET /api/navigation/nearby?lat=...\&lon=... 请求。  
* \[ \] **视觉标记**：后端返回的假数据/真实数据点位，能够以蓝色标记的形式（区分于普通地图 POI）渲染在高德地图上。  
* \[ \] **街景弹窗**：点击上述蓝色标记，能成功弹出 StreetViewModal，且弹窗内正确渲染了该点位的 scene\_description 以及 8 个方位的图片切换逻辑。

#### **4\. 导航推演的战前准备 (对应模块 2.4)**

* \[ \] **起终点选取**：在地图任意位置右键，能呼出菜单并成功设置“起点”和“终点”（地图上出现对应的绿色和红色标记）。  
* \[ \] **全局状态更新**：在控制台输入 window.appState，确认 origin 和 destination 对象的 lng, lat 属性被正确赋值。  
* \[ \] **核心按钮联动**：左侧面板的“生成推演播报”按钮在起终点未完全设置时处于禁用/置灰状态；只有当起点和终点都设置完毕后，按钮才变为可点击的启用状态。